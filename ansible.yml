Ansible playbook written by devops engineer 



If you use ansible-doc yum to look at the syntax for the yum module, you'll see
that its name directive will take a list of packages that the module should work
